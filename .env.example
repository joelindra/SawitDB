# SawitDB Server Configuration Example
# Copy this file to .env and modify as needed

# Server Configuration
SAWIT_PORT=7878
SAWIT_HOST=0.0.0.0
SAWIT_DATA_DIR=./data

# Authentication (optional)
# Format: username:password
# SAWIT_AUTH=admin:secret123

# Performance
# SAWIT_MAX_CONNECTIONS=100
# SAWIT_QUERY_TIMEOUT=30000

# Logging
# SAWIT_LOG_LEVEL=info
# Options: debug, info, warn, error

# DURABILITY (WAL)
# SAWIT_WAL_ENABLED=true
# SAWIT_WAL_SYNC_MODE=normal
# Options: off (fastest), normal (balanced), full (safest)
# SAWIT_WAL_CHECKPOINT_INTERVAL=10000

# MULTI-THREADING (Worker Pool)
# SAWIT_CLUSTER_MODE=true
# SAWIT_CLUSTER_WORKERS=0 
# 0 = Auto-detect CPU cores
